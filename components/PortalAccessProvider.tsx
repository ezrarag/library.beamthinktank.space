'use client'
import { createContext,useContext,useEffect,useState,type ReactNode } from 'react'
import type { User } from 'firebase/auth'; import { doc,onSnapshot,setDoc,serverTimestamp } from 'firebase/firestore'
import { db,subscribeToAuth } from '@/lib/firebase'; import type { PortalRole } from '@/lib/ngoConfig'; import { libraryConfig } from '@/lib/ngoConfig'; import { normalizePortalRole } from '@/lib/resolvePortalPath'
type State={status:'loading'|'signed-out'|'authenticated';user:User|null;role:PortalRole}; const initial:State={status:'loading',user:null,role:'member'}; const Context=createContext(initial); export const usePortalAccessState=()=>useContext(Context)
export function PortalAccessProvider({children}:{children:ReactNode}){const[state,setState]=useState(initial);useEffect(()=>{let stop=()=>{};const unsubscribe=subscribeToAuth(user=>{stop();if(!user){setState({...initial,status:'signed-out'});return}setState({status:'authenticated',user,role:'member'});if(!db)return;const ref=doc(db,'memberships',user.uid);void setDoc(ref,{ngoSlug:'library',updatedAt:serverTimestamp()},{merge:true});stop=onSnapshot(ref,snapshot=>{const role=normalizePortalRole(snapshot.data()?.role,libraryConfig)||'member';setState({status:'authenticated',user,role})},()=>undefined)});return()=>{unsubscribe();stop()}},[]);return <Context.Provider value={state}>{children}</Context.Provider>}
