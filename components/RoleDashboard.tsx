import { PortalShell } from '@/components/PortalShell'; import { libraryConfig,type PortalRole } from '@/lib/ngoConfig'; const copy:Record<PortalRole,string>={member:'Discover saved materials, collections, and learning opportunities.',curator:'Steward records, archival workflows, and collection context.',partner:'Coordinate institutional access, opportunities, and collection partnerships.',civic:'Connect public memory, community research, and civic learning.'};export function RoleDashboard({role}:{role:PortalRole}){return <PortalShell config={libraryConfig} title={`${role[0].toUpperCase()+role.slice(1)} dashboard`} description={copy[role]}><div className="grid gap-4 md:grid-cols-3">{['Workspace','Activity','Resources'].map(label=><div key={label} className="rounded-3xl border border-white/10 bg-black/10 p-5"><p className="font-medium">{label}</p><p className="mt-2 text-sm text-white/55">This role dashboard is ready for its next implementation pass.</p></div>)}</div></PortalShell>}
