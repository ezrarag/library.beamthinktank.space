'use client'
import type { ReactNode } from 'react'; import Link from 'next/link'; import { Lock } from 'lucide-react'; import { usePortalAccess } from '@/lib/usePortalAccess'; import type { NGOConfig } from '@/lib/ngoConfig'
export function PortalShell({config,title,description,children}:{config:NGOConfig;title:string;description:string;children:ReactNode}){const access=usePortalAccess(config);if(access.status==='loading')return <div className="page"><section className="surface-panel p-8">Checking your portal session...</section></div>;if(access.status==='signed-out')return <div className="page"><section className="surface-panel p-8"><Lock/><h1 className="mt-4 text-4xl font-semibold">{title}</h1><p className="mt-4 text-white/70">{description}</p><Link className="button mt-6" href={access.signInUrl}>Sign in through BEAM Home</Link></section></div>;return <div className="page"><section className="surface-panel p-8"><p className="eyebrow">{access.role} workspace</p><h1 className="mt-4 text-4xl font-semibold">{title}</h1><p className="mt-4 text-white/70">{description}</p><div className="mt-8">{children}</div></section></div>}
