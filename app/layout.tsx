import type { Metadata } from 'next'; import type { ReactNode } from 'react'; import { AuthBootstrapper } from '@/components/AuthBootstrapper'; import { SiteFrame } from '@/components/SiteFrame'; import { libraryConfig } from '@/lib/ngoConfig'; import './globals.css'
export const metadata:Metadata={title:libraryConfig.name,description:libraryConfig.description};export default function Layout({children}:{children:ReactNode}){return <html lang="en"><body><AuthBootstrapper/><SiteFrame config={libraryConfig}>{children}</SiteFrame></body></html>}
