import type { Metadata } from 'next'; import type { ReactNode } from 'react'; import { AuthBootstrapper } from '@/components/AuthBootstrapper'; import { AppHeader } from '@/components/AppHeader'; import { SiteFooter } from '@/components/SiteFooter'; import { libraryConfig } from '@/lib/ngoConfig'; import './globals.css'
export const metadata:Metadata={title:libraryConfig.name,description:libraryConfig.description};export default function Layout({children}:{children:ReactNode}){return <html lang="en"><body><AuthBootstrapper/><div className="flex min-h-screen flex-col"><AppHeader config={libraryConfig}/><main className="flex-1">{children}</main><SiteFooter config={libraryConfig}/></div></body></html>}
